'use client';

import { Toaster } from '@/components/ui/sonner';

/**
 * PRD: ToastProvider 포함 (store layout).
 * shadcn sonner 사용.
 */
export default function ToastProvider() {
  return <Toaster />;
}
