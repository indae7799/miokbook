{
  "version": 2,
  "installCommand": "corepack enable && corepack prepare pnpm@9.14.2 --activate && cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm build",
  "framework": "nextjs"
}
