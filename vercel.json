{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@9.14.2 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "cd apps/web && pnpm run build",
  "outputDirectory": "apps/web/.next"
}
